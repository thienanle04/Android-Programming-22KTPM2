<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <Preference
        android:key="google_sign_in"
        android:title="Sign in with Google"
        android:summary="Tap to sign in with your Google account" />

    <SwitchPreferenceCompat
        app:key="notifications"
        app:title="Enable message notifications"/>

    <Preference
        app:key="set_hide_password"
        app:title="Set Hidden Album Password"
        app:summary="Set or change password for hidden albums" />

    <Preference
        app:key="set_lock_password"
        app:title="Set Lock Password"
        app:summary="Set or change password to lock albums" />

    <!-- Số cột trong chế độ dọc (Portrait) -->
    <EditTextPreference
        app:key="span_count_portrait"
        app:title="Columns (Portrait)"
        app:summary="Set number of columns in portrait mode"
        app:dialogTitle="Enter number of columns (Portrait)"
        app:defaultValue="3"
        android:inputType="number" />

    <!-- Số cột trong chế độ ngang (Landscape) -->
    <EditTextPreference
        app:key="span_count_landscape"
        app:title="Columns (Landscape)"
        app:summary="Set number of columns in landscape mode"
        app:dialogTitle="Enter number of columns (Landscape)"
        app:defaultValue="6"
        android:inputType="number" />

    <Preference
        app:key="feedback"
        app:title="Send feedback"
        app:summary="Report technical issues or suggest new features"/>

</PreferenceScreen>