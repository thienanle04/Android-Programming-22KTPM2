<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme using Material 3 -->
    <style name="Base.Theme.Group3" parent="Theme.Material3.DayNight.NoActionBar">

    </style>

    <style name="Theme.Group3" parent="Base.Theme.Group3" />

    <!-- BottomSheetDialog with Transparent Background (Use Material 2) -->
    <style name="CustomBottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item> <!-- Removes dim background -->
    </style>
</resources>
